services:
  - type: web
    name: retail-ops-intelligence
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run retail-ops-intelligence/dashboard/app.py --server.port $PORT --server.address 0.0.0.0
